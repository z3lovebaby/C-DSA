#include<stdio.h>
int main(){
  int a,b,c,max;
  printf("Nhap vao 3 so: ");
  scanf("%d%*c%d%*c%d%*c",&a,&b,&c);
  max = a>b ? a:b;
  max = max <c ? c:max;
  printf("Max = %d\n",max);
  return 0;


}
