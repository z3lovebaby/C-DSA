#include<stdio.h>
#include<string.h>
int check(char *a,char *b){
  int l1,l2,t;
  l2=strlen(a);
  l1=strlen(b);
  while (l2!=0){
    if (*(b+l2)!=*(a+l1)) return 0;
    l2--;
    l1--;
  }
  return 1;
}
int main(){
  
  return 0;
}
