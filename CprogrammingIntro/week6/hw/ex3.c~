#include<stdio.h>
#include<time.h>
#include<stdlib.h>
int main(){
  int urN,lucky;
  srand((unsigned)time(0));
  lucky = rand() % 1000000;
  printf("Nhap so ma ban muon chon: ");
  scanf("%d",&urN);
  printf("%d \t%06d\n",lucky,urN);

  return 0;
}
