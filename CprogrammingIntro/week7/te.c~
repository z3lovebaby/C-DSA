#include<stdio.h>
int main(){
  /*int i,x;
  for (i=1;i<=100;i++){
    x+=i;
    printf("%d\n",i);
    if (x % i == 0){
      i--;
      printf("%d\n",i);
    }

  }
  */
  char c;
  int count;
  for (count=0; (c=getchar()!='.'); count++)
    { printf("%d\t",count);}
  printf("Number of characters is %d\n", count);
  return 0;
}
